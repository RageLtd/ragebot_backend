import { TwitchNotification } from "../notifications/notifications";

function sendMessage(
  url: string,
  notification: TwitchNotification,
  notificationString: string
) {}

export default {
  sendMessage,
};
